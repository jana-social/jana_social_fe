<html>
<head>
  <title>Search Show Page</title>
  <style>
    .content-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    .image-container {
      width: 500px; /* Adjust the width as needed */
      margin: 0 auto; /* Center the container horizontally */
      border: 5px solid #ddd;
      border-radius: 5px;
      padding: 0px;
      text-align: center;
    }

    .button-container {
      margin-top: 0px;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 25vh;
    }

    .accept-button {
      background-color: #4caf50; /* Green */
      color: white;
      padding: 15px 35px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .reject-button {
      background-color: #ff5733; /* Red */
      color: white;
      padding: 15px 30px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    /* New styles for highlighted username and separate colors */
    .username {
      color: #3366cc; /* Highlighted color for the username */
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 10px;
    }

    .user-details {
      color: #666666; /* Color for user details */
      font-size: 18px;
    }

    /* Header style */
    .masthead {
      background-color: #222; /* Dark gray */
      padding: 130px 0;
    }

    .masthead-heading {
      font-size: 48px;
      margin-bottom: 20px;
    }

    .divider-custom-line,
    .divider-custom-icon {
      background-color: #666666; /* Divider color */
    }

    /* Icon color */
    .divider-custom-icon i {
      color: #ffcc00;
    }

    /* Added styles for boxes */
    .wide-box {
      width: 460px; /* Adjust the width as needed */
    }
    .user-details-box {
      border: 5px solid #ddd;
      padding: 15px;
      border-radius: 5px;
      margin-top: 20px;

    }
  </style>
</head>
<body>
  <header class="masthead bg-primary text-white text-center">
    <div class="container d-flex align-items-center flex-column">
      <h1 class="masthead-heading mb-0">Jana Social</h1>
      <div class="divider-custom divider-light">
        <div class="divider-custom-line"></div>
        <div class="divider-custom-icon"><i class="fas fa-star"></i></div>
        <div class="divider-custom-line"></div>
      </div>
    </div>
  </header>
  <div class="content-container"><br>
    <h2 class="username"><%= @user.username %></h2><br>

    <div class="image-container">
      <img src="<%= @user.photo_url %>" class="img-thumbnail">
    </div>

    <div class="user-details-box">
      <ul class="user-details wide-box">
        <li><b>Likes:</b> <%= @user.likes %></li><br>
        <li><b>Dislikes:</b> <%= @user.dislikes %></li><br>
        <li><b>Bio:</b> <%= @user.bio %></li><br>
      </ul>
    </div>
    <div class="button-container">
      <div style="display: flex; gap: 20px;">
        <%= button_to "Accept", search_users_path(user_id: session[:user_id], friend_id: @user.id, status: 'approved'), method: :post, class:"accept-button" %>

        <%= button_to "Reject", search_users_path(user_id: session[:user_id], friend_id: @user.id, status: 'declined'), method: :post, class:"reject-button" %>
      </div>
    </div>
  </div>
</body>
</html>


